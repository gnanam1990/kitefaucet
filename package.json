{
  "name": "kitefaucet",
  "private": true,
  "version": "0.1.0",
  "description": "Testnet faucet for Kite devs — drip native KITE + Test USDT with sane rate limits",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r exec tsc --noEmit"
  },
  "packageManager": "pnpm@9.12.0"
}
